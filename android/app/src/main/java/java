package com.yourcompany.yourapp;

import io.flutter.embedding.android.FlutterActivity;

public class MainActivity extends FlutterActivity {  // ← Must extend FlutterActivity
    // No need to override anything
} 
